services:
  postgres:
    image: postgres:15
    container_name: ai_lead_postgres
    restart: always
    environment:
      POSTGRES_USER: harunhardal
      POSTGRES_PASSWORD: 441997Hh.
      POSTGRES_DB: ai_lead_db
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  redis:
    image: redis:7
    container_name: ai_lead_redis
    restart: always
    ports:
      - "6379:6379"

volumes:
  postgres_data: